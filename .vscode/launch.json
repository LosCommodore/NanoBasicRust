{
    "version": "0.2.0",
    "$schema": "vscode://schemas/launch",
    "configurations": [
        {
            "name": "Debug Rust (Linux)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/cs_rust",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "rust: cargo build",
            "sourceLanguages": ["rust"]
        }
    ]
}
