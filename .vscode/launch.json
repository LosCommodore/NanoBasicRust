{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust (MSVC)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\target\\debug\\${workspaceFolderBasename}.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "preLaunchTask": "rust: cargo build"
        }
    ]
}
