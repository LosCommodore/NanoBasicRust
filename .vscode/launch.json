{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust (MSVC)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\target\\debug\\cs_rust.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "preLaunchTask": "rust: cargo build"
        }
    ]
}
