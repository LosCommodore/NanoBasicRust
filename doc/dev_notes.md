#  Clear rust alayzer cache if error
```
rust-analyzer language server: WARN Propagating panic for cycle head that panicked in an earlier execution in that revision
```

rm -rf ~/.cache/rust-analyzer/